/*!
 * @file
 * @brief Miniz export macro stub for ESP-IDF builds.
 *
 * Miniz requires this header for function visibility. Since we use
 * MINIZ_HEADER_FILE_ONLY (inline implementation), no export is needed.
 */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT

#endif /* MINIZ_EXPORT_H */
